server:
  port: 8080

spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:lm-studio}
      base-url: ${OPENAI_BASE_URL:http://localhost:1234/v1}
      chat:
        options:
          model: ${OPENAI_MODEL:qwen2.5-72b-instruct}
          temperature: 0.7

flexagent:
  mcp:
    enabled: true
